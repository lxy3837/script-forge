# ScriptForge 使用说明

> 基于 Java + DeepSeek 的多 Agent 二次元游戏二创剧本自动生成系统

---

## 一、环境准备

### 1.1 安装 JDK 17+

下载并安装 [JDK 17](https://adoptium.net/download/) 或更高版本，安装后验证：

```powershell
java -version
```

### 1.2 安装 Maven（可选）

项目自带 Maven Wrapper（`mvnw.cmd`），无需单独安装。如果已安装 Maven 3.9+，也可直接使用 `mvn` 命令。

### 1.3 获取 DeepSeek API Key

1. 打开 [platform.deepseek.com](https://platform.deepseek.com)，用手机号或邮箱注册账号并登录
2. 进入左侧菜单 **API Keys**，点击「创建 API Key」
3. 给 Key 起个名字（如 `script-forge`），点击创建
4. **立即复制**生成的 Key（格式：`sk-xxxxxxxx`），关闭后不可再次查看
5. 进入「充值」页面，建议至少充 10 元（deepseek-v4-pro 生成完整剧本约消耗 1~3 元/次）

---

## 二、密钥与配置

所有配置集中在 `src/main/resources/application.yml`，以下是各配置项的详细说明。

### 2.1 必须修改的配置

```yaml
deepseek:
  api-key: ${DEEPSEEK_API_KEY:your-api-key-here}   # ← 替换为你自己的 API Key
  base-url: https://api.deepseek.com              # API 地址，一般无需改动
```

### 2.2 DeepSeek 对话参数

```yaml
deepseek:
  chat:
    model: deepseek-v4-pro       # 对话模型，也可改为 deepseek-chat（更便宜）
    temperature: 0.7             # 创意度，0~1，越高越随机
    max-tokens: 32768            # 单次最大输出 Token
    timeout-seconds: 600         # 超时时间（秒），生成剧本较慢，不建议改小
  embedding:
    model: deepseek-embed        # 向量模型，用于角色人设检索
    timeout-seconds: 30
  max-retries: 3                 # 失败自动重试次数
```

### 2.3 服务与数据库

```yaml
server:
  port: 8080                     # 服务端口，冲突可改

spring:
  datasource:
    url: jdbc:h2:file:./data/scriptforge;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=MySQL
    username: sa
    password: ''                 # H2 默认无密码
  jpa:
    hibernate:
      ddl-auto: update           # 自动建表/更新表结构
```

### 2.4 应用参数

```yaml
app:
  name: ScriptForge
  version: 1.0.0
  export-dir: ./exports                        # 导出文件保存目录
  max-concurrent-projects: 3                   # 最大并发项目数
  max-outline-versions: 3                      # 每次生成的大纲版本数
  min-chapters-per-outline: 3                  # 每套大纲最少章节数
  disk-space-min-gb: 2                         # 最低磁盘空间要求

vector-db:
  dimension: 1024                              # 向量维度，与DeepSeek Embedding保持一致
  index-dir: ./data/lucene-index               # 向量索引存储目录
  hnsw:
    m: 16
    ef-construction: 200

logging:
  file:
    name: ./logs/script-forge.log              # 日志文件路径
  level:
    com.erchuang.scriptforge: DEBUG            # 项目包日志级别
    root: INFO
```

### 2.5 密钥加密（可选，更安全）

项目已集成 Jasypt 加密。如果你不想明文存储 API Key：

**步骤 1** — 用 Jasypt 加密你的 Key：

```powershell
# 进入项目目录
cd g:\Java\java1\script-forge

# 用 Maven 运行 Jasypt 加密（密码为 ScriptForge@2024!）
mvn jasypt:encrypt-value -Djasypt.encryptor.password="ScriptForge@2024!" -Djasypt.plugin.value="sk-你的真实Key"
```

**步骤 2** — 将 `application.yml` 中的明文 Key 替换为密文：

```yaml
deepseek:
  api-key: ENC(上面命令输出的密文)
```

**步骤 3** — 设置环境变量（二选一）：

```powershell
# 方式A：系统环境变量
setx JASYPT_ENCRYPTOR_PASSWORD "ScriptForge@2024!"

# 方式B：启动时传入
java -Djasypt.encryptor.password="ScriptForge@2024!" -jar target\script-forge-1.0.0.jar
```

---

## 三、构建与启动

### 3.1 编译打包

```powershell
# 在项目根目录执行
mvnw.cmd clean package -DskipTests
```

编译成功后，`target/` 目录下会生成 `script-forge-1.0.0.jar`。

### 3.2 启动服务

```powershell
java -jar target\script-forge-1.0.0.jar
```

控制台输出 `Started ScriptForgeApplication` 即启动成功。

### 3.3 打开浏览器

访问 [http://localhost:8080](http://localhost:8080)

---

## 四、使用流程

### 4.1 首页 — 创建项目

1. 点击「新建项目」
2. 输入项目标题（如"钟离与雷电将军的璃月相遇"）
3. 输入游戏名称（如 `原神`）
4. 点击「创建」，进入项目工作台

### 4.2 启动工作流

点击「启动工作流」，系统自动执行 7 步流水线：

```
INIT → 需求分析 → 联网检索+角色人设 → 大纲设计 → 剧本生成 → 质量审核 → 导出
```

每个步骤的进度通过 WebSocket 实时推送到前端。

### 4.3 用户确认节点

工作流在以下环节会**暂停等你确认**：

| 节点 | 操作 |
|------|------|
| 需求确认 | 阅读 Agent 补全的需求描述，不满意可输入反馈重新生成 |
| 大纲选择 | 系统生成多套大纲，选择最满意的一套 |
| 分支选择 | 剧本章节有多分支时，选择剧情走向 |

### 4.4 查看成果

| 页面 | 路径 | 内容 |
|------|------|------|
| 项目工作台 | `/projects/{id}` | 全流程概览 |
| 剧本阅览 | `/projects/{id}/script-viewer` | 完整分镜剧本 |
| 审核报告 | `/projects/{id}/review-report` | OOC 检测 + 逻辑检查 + 节奏分析 |

### 4.5 导出文档

在工作台点击「导出」，支持 5 种格式：

| 格式 | 扩展名 | 适用场景 |
|------|--------|----------|
| Markdown | `.md` | 技术文档、GitHub |
| Word | `.docx` | 编辑排版 |
| PDF | `.pdf` | 交付归档 |
| SRT 字幕 | `.srt` | 视频配音 |
| 纯文本 | `.txt` | 通用阅读 |

导出的文件保存在 `./exports/` 目录。

---

## 五、知识库管理

访问 [http://localhost:8080/knowledge](http://localhost:8080/knowledge) 可管理：

- **角色卡片** — 手动添加角色人设，自动生成向量索引用于角色检索 Agent 匹配
- **知识条目** — 添加世界观设定、剧情背景等参考资料

---

## 六、常见问题

### Q1: 启动报 `Address already in use: bind`

端口 8080 被占用，修改 `application.yml` 中 `server.port` 为其他端口（如 8081）。

### Q2: API 调用报 401 或超时

- 确认 `api-key` 正确且账户有余额
- 确认网络能访问 `https://api.deepseek.com`
- 完整剧本生成约需 3~10 分钟，请耐心等待
- 可尝试将 model 改为 `deepseek-chat`（更快更便宜）

### Q3: 如何重置所有数据？

删除 `data/` 目录即可（包含 H2 数据库和向量索引）。

### Q4: 如何查看日志？

日志文件：`./logs/script-forge.log`

### Q5: 导出的 Word/PDF 中文乱码？

系统使用 POI 和 PDFBox，应无乱码问题。如遇到，请确认操作系统已安装中文字体。

---

## 七、目录结构

```
script-forge/
├── pom.xml                          # Maven 项目文件
├── mvnw.cmd                         # Maven Wrapper（Windows）
├── README.md                        # 项目说明
├── 使用说明.md                       # 本文件
├── src/
│   ├── main/
│   │   ├── java/com/erchuang/scriptforge/
│   │   │   ├── ScriptForgeApplication.java      # 启动入口
│   │   │   ├── agent/                           # Agent 模块（8个）
│   │   │   ├── config/                          # 配置类
│   │   │   ├── export/                          # 导出引擎
│   │   │   ├── llm/DeepSeekClient.java          # DeepSeek API 客户端
│   │   │   ├── model/                           # 实体/DTO/枚举
│   │   │   ├── service/                         # 业务服务
│   │   │   ├── vectordb/                        # Lucene 向量检索
│   │   │   └── web/controller/                  # REST API + 页面路由
│   │   └── resources/
│   │       ├── application.yml                  # 配置文件
│   │       ├── prompts/                         # LLM Prompt 模板
│   │       ├── templates/                       # Thymeleaf 页面
│   │       └── static/                          # CSS/JS 静态资源
│   └── test/                                    # 单元测试
├── data/                                        # 运行时数据（数据库+索引）
├── logs/                                        # 日志文件
├── exports/                                     # 导出文件
└── workspaces/                                  # 项目工作空间
```
